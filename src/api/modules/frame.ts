import { broadcast, send, useFocusedWindow } from '../../main/lib/frame'
import { notify } from '../../main/utils/notification'

export * from '../shim'

export {
  useFocusedWindow,
  send,
  broadcast,
  notify,
}
