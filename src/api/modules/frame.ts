import { broadcast, useFocusedWindow } from '../../main/lib/frame'
import { notify } from '../../main/utils/notification'

export * from '../shim'

export {
  useFocusedWindow,
  broadcast,
  notify,
}
