import { getAddonPaths, resolveAddon } from '../../main/lib/addon'
import { userData } from '../../main/utils/directory'

export * from '../shim'

export {
  userData,
  getAddonPaths,
  resolveAddon,
}
